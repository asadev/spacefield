export const metadata = {
  title: "CAC / LTV Calculator",
  description: "Customer acquisition cost, lifetime value, payback, LTV:CAC ratio.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
