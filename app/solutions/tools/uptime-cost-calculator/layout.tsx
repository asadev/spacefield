export const metadata = {
  title: "Uptime Cost Calculator",
  description:
    "Estimate the cost of downtime and compare annual cost at current SLA vs the next tier.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
