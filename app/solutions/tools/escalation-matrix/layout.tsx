export const metadata = {
  title: "Escalation Matrix",
  description:
    "Who gets paged when. Per-severity notification lists with timing — print-ready.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
