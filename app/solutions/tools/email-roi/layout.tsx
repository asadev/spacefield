export const metadata = {
  title: "Email Campaign ROI Calculator",
  description:
    "Project revenue, profit, ROI and funnel for an email campaign. Preset templates for promotional, newsletter, and transactional sends.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
