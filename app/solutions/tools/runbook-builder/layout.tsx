export const metadata = {
  title: "Runbook Builder",
  description:
    "Structured incident runbooks: triggers, checks, mitigation, escalation, rollback.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
