export const metadata = {
  title: "QR Code Generator",
  description: "Generate a QR code from any URL or text. Download as SVG.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
