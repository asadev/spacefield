export const metadata = {
  title: "Content Brief Builder",
  description: "Generate a writer-ready content brief with title options, outline, word targets, and meta.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
