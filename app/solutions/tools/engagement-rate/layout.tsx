export const metadata = {
  title: "Social Engagement Rate Calculator",
  description:
    "Platform-specific engagement rate for Instagram, X, LinkedIn, TikTok, and YouTube with 2026 benchmarks.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
