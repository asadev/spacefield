export const metadata = {
  title: "Word Count",
  description: "Characters, words, sentences, paragraphs, reading time, speaking time, keyword density.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
