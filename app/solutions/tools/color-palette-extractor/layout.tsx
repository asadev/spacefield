export const metadata = {
  title: "Color Palette Extractor",
  description:
    "Upload an image and extract dominant colors via k-means clustering. Export as CSS variables, Tailwind config, or JSON.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
