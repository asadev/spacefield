export const metadata = {
  title: "Base64 Encoder / Decoder",
  description: "Encode or decode text and files to Base64 and URL-safe Base64 — locally.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
