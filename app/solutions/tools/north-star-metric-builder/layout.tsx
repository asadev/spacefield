export const metadata = {
  title: "North Star Metric Builder",
  description:
    "Define and track one north-star metric. Weekly targets, variance, trend.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
