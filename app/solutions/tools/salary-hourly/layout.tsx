export const metadata = {
  title: "Salary to Hourly Converter",
  description: "Convert between annual, monthly, and hourly compensation.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
