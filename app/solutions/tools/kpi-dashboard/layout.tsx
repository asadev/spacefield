export const metadata = {
  title: "Support KPI Dashboard",
  description:
    "Track first-response, resolution, CSAT, NPS, volume and backlog. Team mode syncs history.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
