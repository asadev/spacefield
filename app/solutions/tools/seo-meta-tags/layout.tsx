export const metadata = {
  title: "SEO Meta Tags Generator",
  description: "Generate HTML meta tags with Google SERP and Twitter card previews.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
