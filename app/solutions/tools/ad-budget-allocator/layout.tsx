export const metadata = {
  title: "Ad Budget Allocator",
  description:
    "Distribute a paid budget across Google, Meta, LinkedIn, TikTok, and YouTube by marginal conversions.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
