export const metadata = {
  title: "Status Page Generator",
  description:
    "Generate a branded, self-contained status page HTML file you can host anywhere.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
