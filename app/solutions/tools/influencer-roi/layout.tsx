export const metadata = {
  title: "Influencer ROI Calculator",
  description:
    "Model reach, engagements, clicks, conversions, and revenue for an influencer partnership. Includes a sensitivity table.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
