export const metadata = {
  title: "Lorem Ipsum Generator",
  description: "Generate placeholder text — classic, hipster, corporate, or pirate variants.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
