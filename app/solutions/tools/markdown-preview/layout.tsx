export const metadata = {
  title: "Markdown Preview",
  description: "Live split-pane markdown editor with GFM support — runs in your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
