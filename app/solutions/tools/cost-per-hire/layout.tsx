export const metadata = {
  title: "Cost-per-Hire Calculator",
  description: "Internal + external hiring costs divided by hires.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
