export const metadata = {
  title: "Sales Commission Calculator",
  description:
    "Tiered commission, SPIFs, and accelerators with SaaS, enterprise AE, and BDR presets. Effective rate + payout breakdown.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
