export const metadata = {
  title: "360 Review Template Generator",
  description: "Build printable 360-degree review forms with behavioral-anchored scales and open-ended prompts.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
