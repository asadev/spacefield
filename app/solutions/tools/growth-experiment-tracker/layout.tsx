export const metadata = {
  title: "Growth Experiment Tracker",
  description:
    "ICE-prioritized experiment backlog. Hypotheses, results, learnings.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
