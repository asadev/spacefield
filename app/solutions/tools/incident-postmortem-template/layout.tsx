export const metadata = {
  title: "Incident Postmortem Template",
  description:
    "Structured PIR generator with severity, timeline, impact, root cause, action items.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
