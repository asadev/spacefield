export const metadata = {
  title: "Savings Goal Planner",
  description:
    "Project how long it takes to reach a savings target with current balance, monthly contributions, and expected return.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
