import { toolMetadata } from "../../_data/meta";
export const metadata = toolMetadata("sales-call-script-builder");
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
