export const metadata = {
  title: "Readability Score",
  description: "Flesch, Flesch-Kincaid, Gunning Fog, SMOG, ARI, Dale-Chall — with sentence-level highlights.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
