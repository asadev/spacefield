export const metadata = {
  title: "Compound Interest Calculator",
  description: "Model investment growth with monthly contributions and compounding.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
