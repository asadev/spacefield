export const metadata = {
  title: "Keyword Difficulty Estimator",
  description:
    "Heuristic keyword difficulty score 0-100 weighted across competitor DR, content length, backlinks, and intent match.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
