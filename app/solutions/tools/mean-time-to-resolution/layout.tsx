export const metadata = {
  title: "Mean Time to Resolution",
  description: "MTTR, MTBF and incident trend analysis from CSV input.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
