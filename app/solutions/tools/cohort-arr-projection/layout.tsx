export const metadata = {
  title: "Cohort ARR Projection",
  description:
    "Project ARR trajectory from monthly cohorts using retention curves.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
