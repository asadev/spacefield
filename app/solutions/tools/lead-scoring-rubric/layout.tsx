export const metadata = {
  title: "Lead Scoring Rubric",
  description:
    "Build a weighted rubric across firmographic and behavioral attributes. Score leads 0-100. Export as JSON.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
