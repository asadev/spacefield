export const metadata = {
  title: "URL Encoder & Parser",
  description:
    "Split a URL into protocol, host, path, and query params. Edit each piece, rebuild the URL, and toggle between encode/decode and base64.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
