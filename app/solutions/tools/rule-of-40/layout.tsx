export const metadata = {
  title: "Rule of 40 Calculator",
  description:
    "SaaS Rule of 40 calculator. Growth rate + margin vs world-class benchmarks.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
