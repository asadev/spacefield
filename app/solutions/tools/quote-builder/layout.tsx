export const metadata = {
  title: "Sales Quote Builder",
  description:
    "Line-item quotes with per-line discounts, expiration, and terms. Save named quotes in-browser. Print or save as PDF.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
