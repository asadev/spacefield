export const metadata = {
  title: "Positioning Canvas",
  description:
    "April Dunford positioning builder. Generates statement and tagline.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
