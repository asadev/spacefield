export const metadata = {
  title: "A/B Test Sample Size",
  description: "Sample size per variant for a given MDE, baseline, confidence.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
