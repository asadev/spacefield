export const metadata = {
  title: "On-call Schedule Builder",
  description:
    "Rotation schedule generator with ICS calendar export. Team mode syncs the schedule.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
