export const metadata = {
  title: "Termination Letter Generator",
  description:
    "Draft an employment termination letter with severance, benefits end date, and return-property reminders. Tone variants included.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
