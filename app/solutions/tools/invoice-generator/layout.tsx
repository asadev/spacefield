export const metadata = {
  title: "Invoice Generator",
  description: "Build a printable invoice. Runs entirely in your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
