export const metadata = {
  title: "Password Strength & Entropy",
  description: "Measure entropy in bits and estimate brute-force time.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
