export const metadata = {
  title: "Break-even Analysis",
  description: "Fixed costs, variable costs, price — units to break even.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
