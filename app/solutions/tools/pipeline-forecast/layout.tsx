export const metadata = {
  title: "Pipeline Forecast",
  description:
    "Weight deals by stage probability and expected close date. Monthly weighted forecast and unweighted pipeline.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
