export const metadata = {
  title: "Hash Generator",
  description: "Compute MD5, SHA-1, SHA-256, SHA-512 of text or files — locally.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
