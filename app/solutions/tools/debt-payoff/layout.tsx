export const metadata = {
  title: "Debt Payoff Calculator — Snowball vs Avalanche",
  description:
    "Compare snowball and avalanche repayment strategies across multiple debts. See payoff dates, total interest, and interest saved.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
