export const metadata = {
  title: "Employee Turnover Rate",
  description: "Annualized turnover rate and cost of churn.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
