export const metadata = {
  title: "Venture Dilution Modeler",
  description:
    "Post-round cap table, dilution per shareholder, option-pool shuffle.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
