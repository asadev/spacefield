export const metadata = {
  title: "Salary Benchmark",
  description: "Compare role salaries across US, UK, UAE and EU markets. Illustrative midpoints based on published ranges.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
