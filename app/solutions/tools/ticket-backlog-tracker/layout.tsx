export const metadata = {
  title: "Ticket Backlog Tracker",
  description:
    "Support ticket pipeline visibility by priority and status. Team mode shares the backlog.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
