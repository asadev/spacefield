export const metadata = {
  title: "Pricing Calculator",
  description:
    "Design 3-5 pricing tiers with features, annual discount, and live preview.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
