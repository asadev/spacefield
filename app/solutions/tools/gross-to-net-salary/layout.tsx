export const metadata = {
  title: "Gross-to-Net Salary Calculator",
  description:
    "Estimate take-home pay across the US, UK, UAE, and Spain. Simplified tax breakdown by contribution type.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
