export const metadata = {
  title: "Font Pairing",
  description:
    "Pick a heading font from 30 curated Google Fonts and get 3-5 body pairings with reasoning, live preview, and copy-ready CSS.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
