export const metadata = {
  title: "Regex Tester",
  description:
    "Pattern + flags with live match highlighting, capture groups table, and replacement preview. Recent expressions saved locally.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
