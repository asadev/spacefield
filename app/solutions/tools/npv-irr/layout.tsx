export const metadata = {
  title: "NPV & IRR Calculator",
  description:
    "Discounted cash flow calculator. Compute NPV, IRR via Newton's method, payback period, and profitability index for any project.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
