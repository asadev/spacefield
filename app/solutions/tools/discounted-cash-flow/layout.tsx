export const metadata = {
  title: "Discounted Cash Flow (DCF)",
  description:
    "5-year DCF with terminal value, WACC sensitivity, and per-share value.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
