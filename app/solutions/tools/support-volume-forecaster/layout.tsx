export const metadata = {
  title: "Support Volume Forecaster",
  description:
    "Forecast 12 weeks of ticket volume and compute required agent headcount and gap.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
