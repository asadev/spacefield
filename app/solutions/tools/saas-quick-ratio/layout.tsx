export const metadata = {
  title: "SaaS Quick Ratio",
  description:
    "Quick Ratio = (new + expansion) / (churn + contraction). Plus NRR.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
