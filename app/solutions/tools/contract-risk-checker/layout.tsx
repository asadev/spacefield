export const metadata = {
  title: "Contract Risk Checker",
  description:
    "Paste a contract and flag risky clauses — unlimited liability, auto-renewal, broad indemnification, perpetual licenses, and more. Runs locally.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
