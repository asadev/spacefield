export const metadata = {
  title: "PTO Accrual Tracker",
  description: "Project PTO, sick, and personal balances. See when you'll hit the cap and how much might forfeit.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
