export const metadata = {
  title: "Runway Scenarios",
  description:
    "Base / best / worst runway model with decision triggers.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
