export const metadata = {
  title: "CSV ↔ JSON Converter",
  description:
    "Paste CSV or JSON and convert to the other. Configurable delimiter, header detection, escaping, and row count.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
