export const metadata = {
  title: "Pirate Metrics Tracker (AARRR)",
  description:
    "Track Acquisition, Activation, Retention, Referral, Revenue over time.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
