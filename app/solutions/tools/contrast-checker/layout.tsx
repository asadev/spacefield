export const metadata = {
  title: "Contrast Checker",
  description:
    "Check WCAG contrast ratios for text and UI components. Nudge the foreground lightness to find the nearest passing shade.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
