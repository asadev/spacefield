export const metadata = {
  title: "Capacity Planner",
  description:
    "Project load 3/6/12 months out, compute required capacity, timeline to limits.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
