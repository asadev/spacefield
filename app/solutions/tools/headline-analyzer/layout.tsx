export const metadata = {
  title: "Headline Analyzer",
  description: "Score headlines 0-100 on length, emotion, sentiment, specificity, and more.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
