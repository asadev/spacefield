export const metadata = {
  title: "SLA Calculator",
  description:
    "Service level math. Allowed downtime by uptime target and period, plus breach detection.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
