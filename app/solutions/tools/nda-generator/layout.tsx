export const metadata = {
  title: "NDA Generator",
  description:
    "Generate a printable mutual or one-way NDA. Runs entirely in your browser — no accounts, no watermarks.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
